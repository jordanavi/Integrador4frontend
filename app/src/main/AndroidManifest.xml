<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jorda.igrejasonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadEntidadeActivity"
            android:label="@string/title_activity_entidade"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadEventoActivity" />
        <activity android:name=".CadLocalActivity" />
        <activity
            android:name=".EventosActivity"
            android:label="@string/title_activity_eventos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IgrejasActivity"
            android:label="@string/title_activity_igrejas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EntidadesActivity"
            android:label="@string/title_activity_entidades"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadIgrejaActivity" />
        <activity android:name=".CadEnderecoActivity"></activity>
    </application>

</manifest>